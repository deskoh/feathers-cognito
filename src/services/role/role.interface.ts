
// Define TypeScript interface for service `role`. (Can be re-generated.)
// !code: imports // !end
// !code: init // !end

export interface RoleBase {
  // !<DEFAULT> code: interface
  name: string;
  // !end
}

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Role extends RoleBase {
  // !code: more
  _id: string;
  // !end
}

// !code: funcs // !end
// !code: end // !end
